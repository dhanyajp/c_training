// write a C function which will accept one input parameter r and returns the perimeter of circle,declare pi as a contant.
#include <stdio.h>
float perimeter(float r)
{
    const float pi=3.14;
    float peri=2*pi*r;
    return perimeter;
}
void main()
{
    int r=9;
    printf("%f",perimeter (r));
}
